{ 
  "log-driver": "json-file",
  "log-opts": {
    "max-size": "500m",
    "max-file": "10",
    "compress": "true",
    "labels": "{{ environment }}",
    "env": "{{ company }},{{ environment }}"
  },
  "metrics-addr" : "0.0.0.0:9323",
  "experimental" : true
}